# Copy to .env and fill in real values.
# The parent repo .env is also loaded as a fallback,
# so STREAM_API_KEY / STREAM_API_SECRET are already set there.

STREAM_API_KEY=your_stream_api_key
STREAM_API_SECRET=your_stream_api_secret

# Only this key needs to be added — it is NOT in the parent .env
# Get a free key at: https://aistudio.google.com/apikey
GOOGLE_API_KEY=AIzaSyCEvPMr7-h5bTTIBKo5QC9eNtkaLoIv5x8
